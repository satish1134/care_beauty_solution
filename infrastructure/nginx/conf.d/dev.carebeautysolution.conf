server {
    listen 80;
    server_name dev.carebeautysolution.com dev.careabeautysolution.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name dev.carebeautysolution.com dev.careabeautysolution.com;

    ssl_certificate /etc/letsencrypt/live/dev.carebeautysolution.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/dev.carebeautysolution.com/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Environment "Development" always;

    location / {
        proxy_pass http://care_app_dev:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
